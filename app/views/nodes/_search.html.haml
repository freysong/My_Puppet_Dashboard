/Nodes搜索表单子程序
= form_tag url_for(:action => action_name), :class => 'search', :method => :get do
  = text_field_tag 'q', params[:q]
  = hidden_field_tag('current', params[:current]) if params[:current].present?
  = hidden_field_tag('successful', params[:successful]) if params[:successful].present?
  %button Search
/仅改变Nodes页面的搜索框
