/某个report点击进去后的页面主程序
#sidebar= render :partial => 'shared/node_manager_sidebar'
#main
  %div{ :class => @report ? @report.kind + '-report' : '' }
    = render :partial => 'report', :locals => { :report => @report }
